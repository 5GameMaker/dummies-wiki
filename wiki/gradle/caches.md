# Caches

## Deleting caches

Caches can be deleted by removing `~/.gradle` directory. Usually you can just delete `~/.gradle/caches`, but
sometimes Gradle gets a bit too broken for that to do anything.
